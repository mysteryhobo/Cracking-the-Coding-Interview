public class Question8 {
	public static void main(String[] args) {
		
	}

	public int find(int sum, int max) {

		int ways = 0;
		for (int i = 0; i * max <= n; i ++) {
			ways += find(sum - i * max, nextMax(max));
		}
		return ways;

	
	public int nextMax(int currMax){
		if (currMax == 25) return 10;
		else if (currMax == 10) return 5;
		else if (currMax == 5) return 1;
		else return 1;
	}


}