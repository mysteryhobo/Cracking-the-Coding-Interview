#include <stdio.h>
#include <stdbool.h>

bool solution1(char str[50]);
int main() { 
	char str[50];
	printf("Please enter the string you wish to check (50 char max):");
	scanf("%s", str);
	printf("result of solution1: %s \n", solution1(str) ? "true" : "false");
	return 0;
}

bool solution1(char str[50]) {
	printf("you entered %s: \n", str);
	bool array[26];
	for (int i) ...
	return true;
}